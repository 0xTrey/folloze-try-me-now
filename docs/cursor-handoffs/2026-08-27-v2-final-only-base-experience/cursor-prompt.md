# Cursor coordinator prompt

Work in `/Users/treyharnden/Projects/folloze-try-me-now-unified-builder` on branch `codex/messaging-compiler-v1`.

Read every file under `docs/cursor-handoffs/2026-08-27-v2-final-only-base-experience/` before editing. Treat the package as the execution contract. Also read the historical compiler package at `docs/cursor-handoffs/2026-08-27-messaging-compiler-autoresearch/` so you extend the accepted compiler instead of rebuilding it.

Implement Work Orders 0 through 8 in order. Define all six recipe contracts, but activate only Product/Solution in production. Use the existing Evidence Graph foundations, messaging compiler, Launch/Guide/Align metadata, section writers, `ExperienceSpecV2`, renderer, brand system, trace, and privacy boundaries. Do not introduce a parallel stack.

The most important product change is final-only rendering. Internal drafts are allowed. The visitor must not receive HTML until one persisted final artifact passes structural and truth gates and is read back. Preserve the 60-second target and reserve at least five seconds for finalization.

Use Cursor subagents for independent contract analysis, lifecycle test authoring, research execution review, recipe fixture design, section-quality review, visual-state implementation, and privacy review if available. Keep one coordinator responsible for integration. Do not recursively broaden scope.

Do not touch, stage, revert, or rewrite these unrelated user files:

- `output/product-owner-remediation/evidence-backed-recommendations.png`
- `output/product-owner-remediation/no-evidence-free-form.png`
- `output/product-owner-remediation/partial-unavailable-brand-fallback.png`

Do not modify root `autoresearch.md`, root `autoresearch.jsonl`, or prior handoff logs. Do not commit, push, deploy, change repository visibility, or read secrets.

Run focused tests after each work order. When implementation is complete, run the required commands in `acceptance.md`, except any command explicitly reserved for independent Codex review. Write `cursor-implementation-report.md` in this package with:

- work order status;
- changed files;
- architecture decisions;
- test commands and exit codes;
- fixture scores and blockers;
- latency results;
- screenshots and artifact paths;
- known limitations;
- exact corrections Codex should consider.

Stop after code, tests, scoped autoresearch baseline, visual evidence, and the report are complete. Return a concise summary with the report path and blockers.
