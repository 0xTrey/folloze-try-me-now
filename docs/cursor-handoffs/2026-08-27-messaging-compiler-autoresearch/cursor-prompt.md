# Cursor coordinator prompt

Work in `/Users/treyharnden/Projects/folloze-try-me-now-unified-builder` on branch `codex/messaging-compiler-v1`.

Read every file in `docs/cursor-handoffs/2026-08-27-messaging-compiler-autoresearch/` before editing. Treat that directory as the execution contract.

Implement the six work orders as one bounded messaging-compiler vertical slice. Use parallel internal subagents for independent contract analysis, benchmark fixture design, trace review, personalization review, and test authoring when your environment supports them. Keep one coordinator responsible for integration. Do not recursively broaden scope.

Preserve the current message spine, Launch/Guide/Align families, 17 archetypes, section writing registry, safe evidence behavior, deterministic renderer, PostHog privacy boundary, and fail-soft preview. Add the missing strategy-candidate, weighted-evaluation, semantic page-plan, compiler receipt, and messaging-quality benchmark layers. Do not create a replacement product stack.

Do not touch, stage, revert, or rewrite these unrelated user files:

- `output/product-owner-remediation/evidence-backed-recommendations.png`
- `output/product-owner-remediation/no-evidence-free-form.png`
- `output/product-owner-remediation/partial-unavailable-brand-fallback.png`

Do not modify the root `autoresearch.md` or root `autoresearch.jsonl`. Do not commit, push, deploy, change repository visibility, or read secrets.

Run focused tests after each work order. Finish by running lint, typecheck, the relevant unit tests, `npm run benchmark:preview`, the new compiler benchmark, both builds, and any focused E2E that can run reliably. If a full gate fails for a pre-existing reason, prove it with a clean baseline comparison rather than hiding it.

Write the implementation report required by the handoff. Stop after the code, tests, benchmark baseline, and report are complete. Return a concise summary with changed files, command results, and remaining blockers so Codex can independently grade the work.

